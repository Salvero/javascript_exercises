var newItems = document.getElementById('li.newItem');

if (newItems.length > 0) {
  for (var i = 0; i < newItem.length; i++) {
    newItem[i].className = 'oldItems';  // change value to old items
  }
}
